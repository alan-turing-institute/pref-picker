# Pref Picker In Java

## Pre-requisites

You will need [Apache Maven installed in your system](https://maven.apache.org/install.html).

## Running the code

From this directory, run the following:

```bash
$ mvn spring-boot:run
```

Then go to `http://localhost:8080/preferencePicker` in your browser.